# STATUS — known issues and what is not verified

A snapshot for anyone picking this up. It records **what is merged but unproven**,
**which documents are wrong**, and **what looks like debris and is not** — so the
next person does not have to rediscover any of it.

⚠ Written 2026-08-26 against `f8b88d0`. If it disagrees with the code, the code wins
and this file is the defect.

## Merged but NOT verified end to end

- **Compose call sites.** Nine call sites now source `container/flock-compose.sh`
  instead of naming `-f container/compose.yaml` directly. The file **parses** in all
  three port states on real Docker, including the fragment `setup.sh` itself writes.
  **No scenario has been run through those call sites.**
- **Nemotron's drain probe.** Both verdicts are unit-proven; it has not been watched
  exiting early against a genuinely empty queue on a live tenant.
- **`accept.sh` install.** Currently **red** at install and fixed only on a branch,
  deliberately unmerged: verifying the fix means running `accept.sh --core`, which is
  the thing it repairs. One run closes this and the compose call sites together.

## ⚠⚠ Documents that are wrong

Each of these does more than go stale — it routes a reader somewhere specific.

| where | what it does |
|---|---|
| `LLD-container.md` §1, §3 | Describes port exposure as static compose config. There are **no `ports:` keys** in the base file; publishing happens through a generated override. An operator running compose directly gets **zero published doors and no error**, then inspects `compose.yaml`, finds no ports, and concludes the file is corrupt. |
| `CONTRACTS.md:348-353` | Says `opened` proves delivery. It proves the **opener returned** — terminal handoff completed. Enter can be swallowed and nothing reads the pane. `TODO` row 77 records **20 `opened` against 16 CLI markers**. |
| `API.md:53` | Promises `correlation_id` is propagated from the request. `api/app.py:651` **mints unconditionally**. External clients will build threading on it and only find out on multi-turn replies. |
| `LLD-watchdog.md:88-102` | Says the switch owns and writes `blocked`. **Contradicted by the same file's invariant 4 at 234-238** and by `watchdog/verification.py:118-125`. |
| `LLD-watchdog.md:217` | Says `WATCHDOG_ENABLED=0` exits the process. It keeps the tailer, sampler and verifier running and silences alerting only. `CONTRACTS.md:956` already states this correctly. |
| `README.md` | Quick start describes setup prompts that no longer exist and states **setup does not start Telegram**, which it now does; the test badge and the agent command list omit `office profiles`. |
| `CHANGELOG.md` | Declares every external contract change belongs in it, then omits **`office profiles`** and the **bundled-client / explicit-publication** contract — so a downstream reader cannot discover what those changes invalidated. |
| `GLOSSARY.md` | Defines `agent`, `port_type`, `provider`, `launch`, `board`, `host` — and is **silent on `profile` and `account`**, whose meaning is settled in `TODO.md` prose. |
| `HLD.md` | Silent on the multi-account/profile mechanism and on the explicit-exposure decision tree. |
| `SPEC-bundled-clients-and-exposure.md:30` | Says both bundled clients run in-tenant; `container/entrypoint.sh` starts **only Telegram**. Also owned by no lane. |
| `SPEC-onboarding-integration.md:75, 96` | Says Phase 2 "never fails" while dead-letters fail; promises every fail carries `failed=N` while the `rc6` category form deliberately carries none. |

## ⚠⚠⚠ Looks wrong. Is not. Do not "fix" these.

- **`docs/build-78-wrong-checker.py`** — an attack fixture: a checker whose negative
  control passes while its oracle can never fail. The name reads like a build leftover.
- **`container/scenarios/tmux-nemotron.sh`** — sits in `scenarios/` and is invoked by
  `accept.sh` nowhere. Deliberate: its header says *manual integration tool, never wire
  into accept.sh*. Emitting no `RESULT` is also deliberate.
- **`inject-partial-control.py`, `fault-forward-unknown.sh`** — scenario-only injectors,
  intentionally outside `src/`, guarded by tests.
- **`container/compose.ports.yaml`** — referenced by scripts, absent from git. It is
  gitignored and generated by `setup.sh` only when ports are published.
- **`NAMING-bus.md`'s v1 envelope rows** — the header binds the inventory to a specific
  commit. It is a **historical snapshot**; "updating" it falsifies the record.
- **`HLD.md` §7's `api :8080 / session :8081`** — the doors always bind those ports
  *inside* the container whatever host port is published. The table is about the
  internal bind and was never affected by the exposure work.
- **`accept.sh`'s LIMIT text** — still accurate, and its subject's absence is
  **deliberate**. `tmux-paste-delivery` joined `main` but **not the `--tmux` suite**,
  which still runs its original three members. ⚠ The LIMIT wording — *"until a
  successful-paste scenario joins this suite"* — reads as a roadmap. **It is not one.**
  Wiring that scenario in is not implied and is not pending; it would be a change to
  the suite's contract and needs deciding on its own merits, not doing because a
  sentence appears to promise it.
- **`TODO.md` rows marked SHIPPED**, and the retained legacy `send_failed` /
  `forward_failed` / `kick_failed` names — deliberate records of why something changed.
- **`POST /agents/all/envelopes` bypassing the roster check** — `all` is the
  deliberate L2 broadcast destination and is not a roster member. To an auditor
  reading route guards this looks like a missing authorisation check; "fixing" it
  breaks broadcast.
- **`GET /agents/{agent}/messages` returning 404 for tmux agents** — mailboxes exist
  only for `port_type: api` clients, which have nowhere else to receive. A tmux
  agent's messages go to its pane. The 404 is the contract, not a regression.
- **`LLD-port-tmux.md` §4's activity-based verification** — it compares
  `pending.verify` against CLI **session-file activity**, and activity near a delivery
  is not receipt of it. That reads as a hole, but the section says explicitly that
  verification never reads the pane and that a rendered pane is not a data source, and
  `verification.py:111-139` **admits activity can false-positive**. The limitation is
  documented, not hidden. ⚠ Whether activity is *sufficient* evidence is a real open
  question — but the document is honest about what it relies on.
- **The profile-login `TODO` row** — stays open. `office profiles` shipping does not
  close it; they are different things.

## Citation checking

`tools/check_citations.py` reports **1 hard failure, 43 near misses**. Three cautions:

1. **Near misses do not fail**, so the count grows unnoticed. It moved by six in a
   single day while every run printed it.
2. **It checks that a line exists, not that the line still says what is claimed.**
   Paths and line ranges are hard-checked for doc targets as well as code — a citation
   past end of file fails. But NEAR/semantic drift needs a backticked symbol to anchor
   on, so a citation pointing at a valid line whose *content* has moved on is invisible.
   One file carries 25 doc citations and reports nothing. **The real drift is larger
   than the number.**
3. **Do not bulk-renumber.** A mass rewrite restores line numbers while losing the
   check that each citation still *describes* what it claims — taking the counter green
   and leaving the drift. Some files are pure line drift and safe to renumber; the only
   way to know is to read them.

## The pattern worth carrying

Several separate defects here share one shape: **something reported success without
doing the work.** An ignored signal disposition making failing subprocesses read as
exit zero. A custody event written for a paste that never landed. A program fed on
stdin that never received it and exited zero anyway. A compose file that could not
load while the test suite passed.

⚠ **A green test count meant nothing in each case, because nothing in CI parsed the
compose file, drove the setup prompts, or checked whether a cited line still says
what the citation claims.** A suite
proves only what it touches — and the useful question is not whether it passes, but
what it never looks at.
