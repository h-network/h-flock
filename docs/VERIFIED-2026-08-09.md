# Verified — plumbing pass, 2026-08-09

> Run against a tenant built from `main`, on the lab host, with **no CLIs** —
> every window a plain shell, so what was under test is h-flock and not an
> agent's judgement. Repeat it with
> [`container/plumbing-check.sh`](../container/plumbing-check.sh).

**24 checks, 24 passing.** This is the rollback point.

## What was exercised

| | |
|---|---|
| **Doors** | `/health`, `/agents`, and `401` with no token |
| **Agent → agent** | `office send` from alice arrived in bob's pane |
| **Boards** | `office add` → ticket on bob's `todo` → `take` → `doing`; four columns served; `tasks.jsonl` written |
| **Clients** | `StartAgent` with `vab: api` enrolled `telegram` — roster row, **no window** |
| **Isolation** | `office peers` did not list the client; a second client's mailbox stayed empty |
| **App → agent** | `POST … {"as":"telegram"}` reached carol's pane as `[message from telegram]` |
| **Agent → app** | `office send -a telegram` landed in the mailbox with `producer: carol` |
| **Cursors** | `after=<cursor>` returned nothing, then exactly the one new message |
| **SSE** | a message sent **while the stream was open** arrived on it |
| **Lifecycle** | `StartAgent` made dave's window, `StopAgent` removed it |
| **Dead-letter** | an envelope to an unenrolled name dead-lettered with a reason |
| **Zombies** | `0` |

## Two things worth writing down

⚠ **A pasted message is executed** in a CLI-less window. `[message from alice] …`
became `bash: [message: command not found`, which is the correct outcome and not
a fault — with no TUI there is nothing to absorb the keystrokes. **The check is
that the text arrived**, so the script greps the pane rather than reading its
exit status.

⚠ **Two apparent failures were the harness, not the system.** Both used `tail -5`
on a captured pane, which returned the blank lines *below* the prompt rather than
the message above it. Verified by hand before concluding anything — the
distinction between a broken system and a broken test is the whole reason to
look rather than to re-run.

## Second pass — builds 16 and 17

Re-run after the profile link, `cloneToAll`, and the window-environment fix.
**24/24 again**, plus:

| checked | result |
|---|---|
| booted agent **without** a profile | `AGENT_NAME`, `AGENT_GUIDE`, `OFFICE_TOOLS`; no config-dir vars |
| booted agent **with** `profile=work` | the same three, plus `CLAUDE_CONFIG_DIR` and `CODEX_HOME` |
| agent **hired over the bus** | identical to the booted one, variable for variable |
| `cloneToAll --dry-run` | listed every tmux agent, wrote nothing |
| `cloneToAll` | 4 cloned, `host` skipped, summary correct |
| every clone's `origin` | the **upstream**, not the first agent's local path |
| `cloneToAll` re-run | 0 cloned, 4 skipped — a clean no-op |

⚠ **The `origin` check is the one that matters.** Agents two, three and four were
cloned from agent one's directory; without the reset their `git push` would write
into agent one's checkout. All four point upstream.

### A 25th check, added because of how build 17 hid

The window-environment drift survived because **each path was individually
correct and individually tested.** Only their *equality* was wrong, and nothing
compared them. So §11 of the check now hires an agent and diffs its environment
against a booted one, agent name normalised.

⚠ **Unit tests could not have caught this and a third one would not either.**
Making `window_env` the single construction site is the fix; comparing the two
paths where they actually run is what stops a fourth path reintroducing it.

Also made the board section idempotent — it took a ticket and never finished it,
so a second run met *"you already have one open task"*. That failure was the
board working exactly as designed, which is the most misleading kind.

## Third pass — build 18, the activity feed

A real claude agent was hired and given work: *list your files, read your
AGENTS.md, report the line count.* Its feed:

```
events: 10   kinds: {input: 1, output: 2, tool: 7}
tools : {Bash: 6, Read: 1}
fields: [agent, cursor, kind, tool, ts, v]
```

The tools match the task — six shell calls and one file read — so the feed
describes what the agent actually did, not what it was asked to do.

| checked | result |
|---|---|
| a working agent produces `tool` events naming the tools | yes, live |
| `GET /activity` before the tailer exists | `200` and an empty list, not `404` |
| an invalid agent name | `404` |
| SSE delivers events while the connection is open | yes, 42 lines |
| **leakage** — paths, command lines, file contents, prompts | **grep clean** |

⚠ **The leakage grep is the check that matters.** This feed is designed to leave
the tenant over HTTP. `fields` above is the whole event: there is no field a tool
argument could occupy, which is a stronger guarantee than filtering one out.

⚠ **A profiled agent could not be tested and that is not a defect.** `sme-2`
carries `profile=work`, so it reads `.claude-work`, which has never been logged
in — `seed_profile_dir` deliberately copies settings, skills and CLAUDE.md but
**not credentials**, because a profile is a separate account. It showed *"Not
logged in · Run /login"*, exactly as designed.

⚠ **`seed-home.sh check` does not know about profile accounts.** It reported all
three CLIs "logged in" while the `work` account was not, because it only looks at
the default paths. The one real finding of this pass.

## Fourth pass — build 20, presence and logging

| checked | result |
|---|---|
| a freshly hired authenticated agent | `idle` — it has not spoken, not "cannot see" |
| a window running no CLI | `unknown` |
| an agent mid-task | `working`, with `last_activity` |
| **records for an envelope sent from inside a window** | **five** — `popped forwarded received opened sent` |

⚠ **`sent` had never reached the container log before this.** It is written to a
file the router tails, the same pattern the board and the activity feed use. The
pane still gets its copy, which is the agent's own confirmation.

⚠ **Two of my own test runs were wrong before the code was.** The first counted
four records because I invoked `office` through `docker exec`, which is not a
window and so has no `FLOCK_LOG_FILE`. The second read `unknown` for a healthy
agent and I nearly filed it as correct — it was the real defect, and only looked
like one because a bare shell gives the same answer.

## Known, unchanged, not regressions

- `sent` is missing from the central log for **agent-sent** envelopes — it is
  written into that agent's pane. Four records appear for every envelope
  (`popped`, `forwarded`, `received`, `opened`); the fifth is in a terminal.
  Parked, with reasons, in [`TODO.md`](TODO.md).
- `GET /agents/<unknown>` answers `200` with zero depths rather than `404`.
  Also parked, also in `TODO.md`.
