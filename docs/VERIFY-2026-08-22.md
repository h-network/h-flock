# Verification round — 2026-08-22

**Base: `main` at `ba0157b`.** Branch `<lane>/verify-2026-08-22`.

⚠ **CODE IS LEADING.** Every row you produce is a doc to fix or a finding to
report — **never a code change to make**. If the code looks wrong, that is a
separate finding for `TODO.md`; do not edit code to match a sentence.

## Why now

Builds 79 through 82 landed in one day: the durable custody mirror, `writer`
provenance, usage and cost records, the widened delivery verification, and a
pile of harness fixes. **The living docs have had no sweep since.** A grep for
today's contracts across every living doc (excluding `BUILD-*` and `VERIFIED-*`,
which are dated records and must not be rewritten):

| contract | living docs mentioning it |
|---|---|
| `writer` | 4 |
| `FLOCK_CUSTODY_FILE` | **0** |
| `delivery.markers` | **0** |
| `usage.requests` | **0** |
| `office usage` | **0** |
| `xrange` on the RESP client | **0** |

⚠ **`office usage` is an operator-facing command with no documentation anywhere.**

## What to do

For **your lane's docs only** (below), read the doc against the code and produce
`docs/VERIFY-2026-08-22-<lane>.md` containing:

1. **Contradictions** — the doc states X, the code does Y. Cite both, `file:line`.
2. **Absences** — a contract that exists in code and appears in no living doc.
3. **Stale citations** — near misses in your files. There are 53 repo-wide; fix
   the ones in **living** docs, leave `BUILD-*` alone.

**Fix what is unambiguous** in your own lane's docs and push it. **Bring anything
that changes a contract, or that you think is a code defect, to me instead.**

## Lanes

| lane | owns |
|---|---|
| **bus** | `LLD-bus-and-switch.md`, `NAMING-bus.md`, and the record/custody sections of `CONTRACTS.md` (§3, §4) |
| **tmux** | `LLD-port-tmux.md`, `LLD-tmux-host.md`, `INVARIANTS-tmux.md`, `NAMING-tmux.md` |
| **api** | `LLD-api.md`, `LLD-session.md`, `API.md`, `INVARIANTS-api.md`, `NAMING-api.md` |
| *architect* | `HLD.md`, `DESIGN-layers.md`, `LLD-container.md`, `GLOSSARY.md`, `CONTRACTS.md` (rest), `DRIFT.md`, `TODO.md`, `CHANGELOG.md` |

## Known starting points, per lane

Not a complete list — they are what one grep found, so **do not treat them as the
scope**.

**bus** — `writer` is on every record (`bus/logging.py:78`); does `CONTRACTS` §3
say so? `mirror()` and the durable evidence file are a new custody guarantee with
no doc. `delivery.markers` is a new per-agent resource. Your own build 80 results
doc is a dated record — leave it.

**tmux** — `port/openers.py` writes `delivery.markers` alongside `pending.verify`,
and neither the port LLD nor the invariants mention it. The verification window
default moved 10 → 120 (`watchdog/service.py`), and `sim-blocked.sh` now derives
its poll from the tenant rather than assuming a constant.

**api** — `office usage [--agent] [--since] [--json]` exists and is undocumented.
`flock/bus/resp.py` gained `xrange`/`xlen`. `container/config/pricing.json` is a
new shipped config file. Decide whether usage belongs in `API.md` at all — it is
not an HTTP surface, and saying so explicitly is a valid finding.

## Rules that have already cost us today

- ⚠ **Do not bulk-rewrite citations mechanically.** I tried it this morning; the
  heuristic matched the word `receive` in a comment and pushed a citation past
  the end of its file, creating a hard failure. Fix them one at a time or leave
  them.
- ⚠ **A `BUILD-*.md` or `VERIFIED-*.md` is correct as history.** Nineteen of them
  now carry a banner saying their figures name no host; that is annotation, not
  rewriting. Do not "correct" their content.
- ⚠ **Read the exit status unpiped.** `python3 tools/check_citations.py` then
  `$?`, before any pipe.

## Done means

Pushed to `origin`, `pytest -q` green, `check_citations.py` exit 0 read unpiped,
and your `docs/VERIFY-2026-08-22-<lane>.md` listing what you found, what you
fixed, and what you are handing to me.

⚠ **No sign-off needed for doc fixes** — this is a sweep, not a build. But if you
find a **code** defect, it goes to me with a locus and it does not get fixed in
this branch.
